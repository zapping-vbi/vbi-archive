Outstanding issues for the next release
---------------------------------------
	+ Write the mpeg plugin (this is a MUST)
	+ VBI can be opened/closed while running, try.
	+ Make the 640x480 auto-choosing configurable.
	+ XGetScreenSaver
	+ make the channel ordering configurable
	+ Fix web page so the table is correctly viewed with Mozilla
	+ Disable audio by default
	+ Bug fixes
	> Release 0.5.5
	+ Subtitles support
	+ Xv support if present.
	+ Support in teletext mode for numeric keys
	? Make the visited pages in the teletext browser a tree ? -
		depends on feedback.
	+ Teletext docs
	> Release 0.5.6

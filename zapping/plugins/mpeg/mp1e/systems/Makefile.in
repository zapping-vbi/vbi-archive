# $Id: Makefile.in,v 1.2 2000-08-10 01:18:59 mschimek Exp $

OBJS	= mpeg1.o mpeg2.o stream.o output.o

all: $(OBJS)
	$(LD) -r $(OBJS) -o systems.o

%.o: %.c
	$(CC) $(CFLAGS) $(DEFS) -c $< -o $@

clean:
	-rm $(OBJS) *.d *% *~ *.bak core log systems.o

%.d: %.c
	@echo Creating $@
	@$(SHELL) -ec '$(CC) -MM $(CFLAGS) $< \
	| sed '\''s/\($*\)\.o[ :]*/\1.o $@ : /g'\'' > $@; \
	[ -s $@ ] || rm -f $@'

-include $(OBJS:.o=.d)
